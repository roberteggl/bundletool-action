name: Continuous Integration

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-typescript:
    name: TypeScript Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        id: setup-node
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint and Format
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm ci-test

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  test-action:
    name: GitHub Actions Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v6

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Test Local Action (dry-run)
        id: test-action-dry-run
        uses: ./
        with:
          aab-file: app/build/outputs/bundle/release/app-release.aab
          sign: false
          dry-run: true
          verbose: true

      - name: Assert dry-run version output
        run: |
          test -n "${{ steps.test-action-dry-run.outputs.bundletool-version }}"
          test -n "${{ steps.test-action-dry-run.outputs.apks-path }}"
          test -n "${{ steps.test-action-dry-run.outputs.apk-path }}"
          echo "Resolved ${{ steps.test-action-dry-run.outputs.bundletool-version }}"
          echo "Planned APKS ${{ steps.test-action-dry-run.outputs.apks-path }}"
          echo "Planned APK ${{ steps.test-action-dry-run.outputs.apk-path }}"

      - name: Test Local Action (install)
        id: test-action-install
        continue-on-error: true
        uses: ./
        with:
          aab-file: app/build/outputs/bundle/release/app-release.aab
          bundletool-version: '1.18.3'
          sign: false
          dry-run: false
          verbose: true

      - name: Assert install outputs
        run: |
          test -n "${{ steps.test-action-install.outputs.bundletool-version }}"
          test -f "${{ steps.test-action-install.outputs.bundletool-path }}"
          java -jar "${{ steps.test-action-install.outputs.bundletool-path }}" version
          # No real AAB in this repo yet; build-apks is expected to fail after install.
          test "${{ steps.test-action-install.outcome }}" = "failure"
